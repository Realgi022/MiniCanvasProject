info:
  name: Auth - Login
  type: http
  seq: 1

http:
  method: POST
  url: http://145.220.72.161/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "admin@admin.nl",
        "password": "rei234hila."
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const body = res.getBody();
        if (body && body.token) {
          bru.setEnvVar("token", body.token);
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
